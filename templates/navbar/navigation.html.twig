<nav
	class="nav-fixed" id="mainNav">

	{# --- TOP BAR --- #}
	<div class="info-banner">
		<div class="info-banner-content">
			<div class="info-track">
				<div class="info-item">
					<i class="fa-solid fa-scale-balanced"></i>
					<span>Association loi 1901</span>
				</div>

				<div class="info-item">
					<i class="fa-solid fa-location-dot"></i>
					<span>Saleux - Complexe sportif</span>
				</div>

				<div class="info-item">
					<i class="fa-solid fa-phone"></i>
					<a href="tel:0322897257">03.22.89.72.57</a>
				</div>

				<div class="info-item">
					<i class="fa-solid fa-handshake"></i>
					<a href="{{ path('app_partenaire') }}">Devenir Partenaire</a>
				</div>
			</div>

			<div class="info-actions">
				<a href="{{ path('faq') }}" class="top-link">
					<i class="fa-solid fa-circle-question"></i>
					<span>Centre d'aide</span>
				</a>

				{% if app.user %}
					<a href="{{ path('app_logout') }}" class="top-link">
						<i class="fa-solid fa-power-off"></i>
						<span>Déconnexion</span>
					</a>
				{% else %}
					<a href="{{ path('app_login') }}" class="top-link">
						<i class="fa-solid fa-user"></i>
						<span>Espace adhérent</span>
					</a>
				{% endif %}
			</div>
		</div>
	</div>

	{# --- MAIN NAV --- #}
	<div class="nav-main-wrapper">
		<div class="nav-content">

			<a href="{{ path('home') }}" class="nav-brand">
				<img src="{{ asset('images/favicon/icon2.png') }}" alt="CHM Saleux" width="40" height="40">
				<span class="brand-text">CHM
					<span class="text-red">SALEUX</span>
				</span>
			</a>

			{# ✅ CTA mobile entre logo et hamburger #}
			{% if app.user %}
				<a href="{{ path('dashboard') }}" class="mobile-register-btn">Mon espace</a>
			{% else %}
				<a href="{{ path('app_auth_choice') }}" class="mobile-register-btn">S'inscrire</a>
			{% endif %}

			<div class="nav-links-desktop">
				<a href="{{ path('home') }}" class="nav-item">Accueil</a>

				<div class="nav-item has-dropdown">
					Club & Membres du Bureau
					<i class="fa-solid fa-chevron-down"></i>

					<div id="club-panel" class="nav-hover-panel">
						<div class="nav-hover-panel-content">
							<div class="panel-column">
								<h3>Services du Club</h3>
								<a href="{{ path('halterophilie') }}">Haltérophilie</a>
								<a href="{{ path('musculation') }}">Musculation</a>
								<a href="{{ path('cours_collectifs') }}">Cours collectifs</a>
								<a href="{{ path('seance_essai') }}">Séance d'essai</a>
								<a href="{{ path('evenements') }}">Événements organisés</a>
								<a href="{{ path('sauna') }}">Sauna</a>
							</div>

							<div class="panel-column">
								<h3>Membres du bureau</h3>
								<a href="{{ path('president') }}">Le Président</a>
								<a href="{{ path('tresorier') }}">Le Trésorier</a>
								<a href="{{ path('secretaire') }}">La Secrétaire</a>
								<a href="{{ path('membres_bureau') }}">Les membres du bureau</a>
							</div>

							<div class="panel-column">
								<h3>A propos de notre club</h3>
								<a href="{{ path('app_club') }}">Présentation du club</a>
								<a href="{{ path('labels_club') }}">Les labels du club</a>
								<a href="{{ path('horaires') }}">Les horaires</a>
							</div>
						</div>
					</div>
				</div>

				<a href="{{ path('app_pricing') }}" class="nav-item">Tarifs</a>
				<a href="{{ path('ecole') }}" class="nav-item">Notre École</a>
				<a href="{{ path('competition') }}" class="nav-item">Compétitions & Résultats</a>
			</div>

			<div class="nav-actions">
				<a id="assistantWidgetOpen" class="btn-icon-ai" title="Assistant IA">
					<img src="{{ asset('images/ia.png') }}" alt="IA">
				</a>

				{% if app.user %}
					<a href="{{ path('dashboard') }}" class="btn-nav-primary">
						<span>MON ESPACE</span>
					</a>
				{% else %}
					<a href="{{ path('app_auth_choice') }}" class="btn-nav-primary">
						<span>S'INSCRIRE</span>
					</a>
				{% endif %}

				<button class="hamburger-btn" id="mobileMenuTrigger" aria-label="Ouvrir le menu">
					<span></span>
					<span></span>
					<span></span>
				</button>
			</div>
		</div>
	</div>

	{# --- MOBILE MENU --- #}
	<div class="mobile-nav-overlay" id="mobileNavOverlay">
		<div class="mobile-nav-panel">

			<div class="mobile-links">
				<a href="{{ path('home') }}">Accueil</a>

				{# ✅ ouvre une page interne (subpage) au lieu d'un accordéon #}
				<button type="button" class="mobile-subpage-trigger" data-subpage="#mobileSubpageClub" aria-label="Voir le club">
					Club & Membres du Bureau
					<i class="fa-solid fa-arrow-right"></i>
				</button>

				<a href="{{ path('app_pricing') }}">Tarifs</a>
				<a href="{{ path('ecole') }}">Notre École</a>
				<a href="{{ path('competition') }}">Compétitions & Résultats</a>
			</div>

			<div class="mobile-footer-nav mobile-footer-nav-redesign">
				{% if app.user %}
					<a href="{{ path('dashboard') }}" class="mobile-footer-link-clean">
						<i class="fa-regular fa-user"></i>
						<span>Mon espace</span>
					</a>
					<a href="{{ path('app_logout') }}" class="mobile-footer-link-clean">
						<i class="fa-solid fa-right-from-bracket"></i>
						<span>Se déconnecter</span>
					</a>
				{% else %}
					<a href="{{ path('app_login') }}" class="mobile-footer-link-clean">
						<i class="fa-regular fa-user"></i>
						<span>Espace adhérent</span>
					</a>
				{% endif %}

				<a href="{{ path('faq') }}" class="mobile-footer-link-clean">
					<i class="fa-regular fa-circle-question"></i>
					<span>Centre d'aide</span>
				</a>
			</div>

			{# --- SUBPAGE : Club & Membres du Bureau --- #}
			<div class="mobile-subpage" id="mobileSubpageClub">
				<button type="button" class="mobile-subpage-back" data-subpage-back aria-label="Retour">
					<i class="fa-solid fa-arrow-left"></i>
					Retour
				</button>

				<div class="mobile-subpage-section">
					<span class="mobile-subpage-label">Services du club</span>
					<a href="{{ path('halterophilie') }}">Haltérophilie</a>
					<a href="{{ path('musculation') }}">Musculation</a>
					<a href="{{ path('cours_collectifs') }}">Cours collectifs</a>
					<a href="{{ path('seance_essai') }}">Séance d'essai</a>
					<a href="{{ path('evenements') }}">Événements organisés</a>
					<a href="{{ path('sauna') }}">Sauna</a>
				</div>

				<div class="mobile-subpage-section">
					<span class="mobile-subpage-label">Membres du bureau</span>
					<a href="{{ path('president') }}">Le Président</a>
					<a href="{{ path('tresorier') }}">Le Trésorier</a>
					<a href="{{ path('secretaire') }}">La Secrétaire</a>
					<a href="{{ path('membres_bureau') }}">Les membres du bureau</a>
				</div>

				<div class="mobile-subpage-section">
					<span class="mobile-subpage-label">À propos</span>
					<a href="{{ path('app_club') }}">Présentation du club</a>
					<a href="{{ path('labels_club') }}">Les labels du club</a>
					<a href="{{ path('horaires') }}">Les horaires</a>
				</div>
			</div>

		</div>
	</div>
</nav>
