{% extends 'base.html.twig' %}

{% block title %}Compétitions & Résultats - CHM Saleux{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/competitions/competitions.css') }}">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
{% endblock %}

{% block body %}

<header class="page-header" style="background-image: url('{{ asset('images/90.jpg') }}');">
    <div class="header-overlay"></div>
    
    <div class="container header-content">
        <a href="{{ path('home') }}" class="back-link">
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
            </svg>
            <span>Retour à l'accueil</span>
        </a>
        
        <div class="header-title-box">
            <span class="subtitle">Saison 2025-2026</span>
            <h1>Compétitions & résultats</h1>
            <p>Le calendrier officiel et les performances de nos athlètes sur les plateaux nationaux et régionaux.</p>
        </div>
    </div>
</header>

<main class="main-container">

    {# --- SECTION AGENDA --- #}
    <section class="calendar-section">
        <div class="section-title-wrapper">
            <h2 class="section-heading">Agenda Sportif</h2>
            <p>Retrouvez les dates clés des prochaines échéances.</p>
        </div>

        <div class="calendar-card">
            <div class="calendar-nav">
                <button class="nav-btn" aria-label="Précédent"><i class="fa-solid fa-chevron-left"></i></button>
                <h3 class="current-month">Décembre 2025</h3>
                <button class="nav-btn" aria-label="Suivant"><i class="fa-solid fa-chevron-right"></i></button>
            </div>
            
            <div class="calendar-grid">
                <div class="day-name">Lun</div>
                <div class="day-name">Mar</div>
                <div class="day-name">Mer</div>
                <div class="day-name">Jeu</div>
                <div class="day-name">Ven</div>
                <div class="day-name">Sam</div>
                <div class="day-name">Dim</div>

                {% for day in 1..31 %}
                    {% set is_competition = day in [5, 12, 20] %}
                    <div class="day-cell {% if is_competition %}has-event{% endif %}">
                        <span class="day-number">{{ day }}</span>
                        {% if is_competition %}
                            <div class="event-indicator">
                                <span class="dot"></span>
                                <span class="event-label">Compétition</span>
                            </div>
                        {% endif %}
                    </div>
                {% endfor %}
            </div>
        </div>
    </section>

    {# --- SECTION ÉQUIPES --- #}
    <section class="teams-section">
        <div class="section-title-wrapper">
            <h2 class="section-heading">Nos Équipes</h2>
            <p>Sélectionnez une équipe pour voir les résultats détaillés.</p>
        </div>

        <div class="teams-grid">
            {# Carte Féminine #}
            <a href="{{ path('competitions_feminine') }}" class="team-card">
                <div class="card-img-box">
                    <img src="{{ asset('images/female-team.jpg') }}" alt="Équipe Féminine">
                    <div class="card-overlay"></div>
                </div>
                <div class="card-info">
                    <span class="pille">Division 1</span>
                    <h3>Équipe<br><strong>Féminine</strong></h3>
                    <p>Résultats, classements et agenda des matchs.</p>
                    <div class="card-footer-link">
                        Explorer <i class="fa-solid fa-arrow-right"></i>
                    </div>
                </div>
            </a>

            {# Carte Masculine #}
            <a href="{{ path('competitions_masculine') }}" class="team-card">
                <div class="card-img-box">
                    <img src="{{ asset('images/male-team.jpg') }}" alt="Équipe Masculine">
                    <div class="card-overlay"></div>
                </div>
                <div class="card-info">
                    <span class="pille">Division 1</span>
                    <h3>Équipe<br><strong>Masculine</strong></h3>
                    <p>Performances, effectifs et calendriers officiels.</p>
                    <div class="card-footer-link">
                        Explorer <i class="fa-solid fa-arrow-right"></i>
                    </div>
                </div>
            </a>
        </div>
    </section>

</main>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/competitions/competitions.js') }}"></script>
{% endblock %}